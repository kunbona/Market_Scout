# ── 核心 Web 框架 ──────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0          # 生产 WSGI 服务器

# ── 数据抓取 ───────────────────────────────────────────────────────────────────
akshare>=1.16.14
requests>=2.31.0
curl_cffi>=0.6.0         # 东方财富 PDF 下载（Chrome 指纹模拟，绕过反爬）
beautifulsoup4>=4.12.0
feedparser>=6.0.11       # RSS 解析
pdfplumber>=0.10.0       # PDF 全文提取（研报）

# ── 调度 & 时间 ────────────────────────────────────────────────────────────────
apscheduler>=3.10.4
pytz>=2024.1

# ── 数据处理 ───────────────────────────────────────────────────────────────────
pandas>=2.0.0

# ── 本地量价（可选，缺少时情绪分析区显示空） ─────────────────────────────────
mootdx>=0.11.0

# ── 投研看板（Research Board）─────────────────────────────────────────────────
anthropic>=0.40.0        # Claude SDK：多模态 API 调用（投研看板 supervisor）
                         # 运行时依赖（pip 安装外，需单独安装）：
                         #   claude CLI：npm install -g @anthropic-ai/claude-code
                         #   codex CLI：npm install -g @openai/codex
