# ── 核心 Web 框架 ──────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0          # 生产 WSGI 服务器

# ── 数据抓取 ───────────────────────────────────────────────────────────────────
akshare>=1.18.57         # A 股数据（涨停池/行业/北向/研报/财新等），需 >=1.18.57 修复 push2delay
requests>=2.31.0
urllib3>=2.0.0           # requests 依赖，需显式列出以使用 Retry adapter
beautifulsoup4>=4.12.0
feedparser>=6.0.11       # RSS 解析（政策 RSS / RSSHub）
python-dateutil>=2.9.0   # policy_rss 时间解析
curl_cffi>=0.6.0         # 东方财富研报 PDF 下载（Chrome TLS 指纹模拟）

# ── 调度 & 时间 ────────────────────────────────────────────────────────────────
apscheduler>=3.10.4
pytz>=2024.1

# ── 数据处理 ───────────────────────────────────────────────────────────────────
pandas>=2.0.0
