# Market Radar：一个 A 股信息聚合 + AI 分析的仪表盘

前段时间做了个盯盘工具，主要给自己用的，顺手开源出来记录一下。

---

<!-- 图片留白：整体仪表盘截图 -->
![overview](./assets/blog/overview.png)

---

## 为什么做这个

最开始不是想做仪表盘的，是想做 AI 分析。

我之前以为 AI 分析的难点在 prompt，后来发现更难的是 UI——我花了好几天给 AI 设计输出格式：JSON 结构、字段名、前端用哪个组件渲染哪个字段。结果就是，AI 每次想说点什么，都被这个格式框住了。比如今天炸板率突然飙升，AI 明明可以展开讲，但格式只有一个 `emotion_score` 字段，只能填"分歧"两个字。

后来改了思路，让 AI 直接写 HTML 报告，前端就是一个 iframe，把 HTML 原样展示。省了我维护格式的麻烦，AI 也不用削足适履了。

---

## AI 分析长什么样

<!-- 图片留白：盘前报告截图 -->
![ai-morning](./assets/blog/ai-morning.png)

一天三次，分盘前、盘中、盘后。

**盘前**的核心是条件触发型预案，不是"关注低空经济"这种，而是"如果低空经济龙头 9:30 后 10 分钟内封板说明主力在，可以介入；如果开盘即跌，今天不做这个方向"。这个格式是逼着 AI 写的，不然它会倾向于给你写总结而不是给你写操作逻辑。

**盘中**主要是把数字翻译成人话。炸板率从 15% 升到 42% 这件事，原始数字你自己也能看到，AI 的价值是告诉你这代表什么——历史上这个位置通常意味着什么，今天和早盘判断比有没有偏离。数字后面跟解释，是我在 skill 里专门约束的。

<!-- 图片留白：盘后报告截图 -->
![ai-evening](./assets/blog/ai-evening.png)

**盘后**是复盘 + 候选标的。候选标的有个约束：代码和名称必须来自当天数据的同一行，不能从记忆里拼凑。这个约束加得有点晚，之前跑出来过几个代码对但名字对不上的情况，排查了才发现是 AI 在乱配对。

有一个地方踩过坑：HTML 里不能出现内部变量名，比如 `zt_pool`、`lhb`、`sector_flow`，这些是数据源的命名，用户完全看不懂。加了个禁用词表，算是治标。

---

## 新闻抓取

<!-- 图片留白：财经快讯 Tab 截图 -->
![news](./assets/blog/news.png)

7 路来源：财联社（含红电报）、金十、格隆汇、东方财富、同花顺、第一财经、华尔街见闻，5 分钟一次，聚合在一个时间线里。

有些来源没有公开 API，走的是 RSSHub 代理。RSSHub 要单独跑一个 Docker 容器，不装的话这几路自动降级，其他功能不受影响。`start.bat` 里做了自动检测，有 WSL2 + Docker 就拉起来，没有就跳过，倒是不用手动处理。

政策动态单独一个 Tab，发改委、证监会、上交所、深交所、财新、巨潮，30 分钟一次，主要看监管动向。

---

## 研报

<!-- 图片留白：研究报告 Tab 截图 -->
![research](./assets/blog/research.png)

东方财富今日研报，每半小时同步，支持按个股/行业/宏观/策略过滤。PDF 可以在页面内直接预览，不需要跳转东财。

东财研报 PDF 有 Referer 校验，直接拿链接访问会 403，服务端做了一层代理转发，把 Referer 带上去，所以预览直接可用。这个细节处理不掉的话，研报功能基本没用。

---

## 市场数据

<!-- 图片留白：市场数据 Tab，涨停池 + 情绪看板 -->
![market](./assets/blog/market.png)

涨停/跌停/炸板/强势股池，5 分钟一刷。行业资金流、概念资金流（387 个概念），15 分钟一刷。龙虎榜、大单异动、北向资金、雪球热度、融资融券、大宗交易、股东人数变化、解禁日历，基本都在跑。

另外有一块情绪分析，用的是本地日线数据：涨停率、炸板率、连板梯队分层、行业密度、换手分布、市值偏好这些，每天收盘后离线计算更新。这块需要自备本地量价数据（`Quant_Data/` 目录），没有的话这个区域显示空，不影响其他 Tab。

全市场涨跌快照 30 秒刷一次，盘中用的，主要看宽度。

---

## 技术栈

React + Flask，SQLite 存数据，APScheduler 定时抓取，AkShare + requests + feedparser 做数据来源。前端 Tailwind CSS，生产用 Waitress。

AI 分析用 Claude，通过 Claude Code 的 skill 系统编排工作流，每次按按钮，Agent 自己去读数据、分析、生成 HTML、写库，不是直接调 API 扔 prompt 那种。

---

## 部署

```bash
git clone -b lite <repo>
cd market-radar
pip install -r requirements.txt
cp .env.example .env.local
# 编辑 .env.local，QUANT_DATA_ROOT 可选，其他基本不用改
cd dashboard && npm install && npm run build && cd ..
bash start.sh
```

访问 `http://localhost:20026`。

Windows 的话直接跑 `start.bat`，会自动处理 RSSHub + WSL2 的部分。

---

*数据来源均为公开可访问的网络接口，仅供个人研究使用。*
