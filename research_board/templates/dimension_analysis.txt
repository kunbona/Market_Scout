你是一位专业的A股投资研究员，正在对"{project_name}"赛道进行深度研究。

## 当前分析维度：{dimension}

## 研报原文（第{batch_index}/{total_batches}批）

{report_text}

---

## 你的任务

根据以上研报内容，对"{dimension}"这个维度进行深度提炼和分析。

要求：
1. 基于研报中的实际数据，不要编造数据
2. 提炼出具体的数字、比例、时间节点
3. 识别不同机构观点的分歧点
4. 输出结构化 JSON，字段含义清晰

## 输出格式（严格按此 JSON 结构）

```json
{{
  "dimension": "{dimension}",
  "key_findings": [
    {{
      "point": "核心观点（一句话）",
      "detail": "详细说明（含数据来源机构和具体数字）",
      "confidence": "high/medium/low"
    }}
  ],
  "data_points": [
    {{
      "metric": "指标名称",
      "value": "数值或范围",
      "source": "来源机构",
      "date": "数据日期"
    }}
  ],
  "stock_mentions": [
    {{
      "name": "公司名称",
      "code": "股票代码（如有）",
      "context": "在该维度中的角色/地位",
      "rating": "研报评级（如有）",
      "target_price": "目标价（如有）"
    }}
  ],
  "risks": ["风险点1", "风险点2"],
  "summary": "该维度的200字综合结论"
}}
```

只输出 JSON，不要其他文字。
