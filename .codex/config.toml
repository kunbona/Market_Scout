# ============================================================
# market-radar — Codex 执行者配置
# 角色：被 Claude Code 调度的研报分析子 Agent
# ============================================================

# 执行者只需读取传入的文字内容，不需要写文件、不需要执行命令
# 设为 read-only 防止误操作，全自动不打断 Claude 的调度流程
sandbox_mode    = "read-only"
approval_policy = "never"

# 项目说明文档
project_doc_max_bytes          = 32768
project_doc_fallback_filenames = ["AGENTS.md"]
